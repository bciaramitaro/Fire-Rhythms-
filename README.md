# Fire-Rhythms-
An interactive system combining Arduino and Max/MSP where real-time load sensor data drives dynamic video playback. The Arduino processes weight sensor input and streams it to Max, which interprets load variations to trigger distinct media states and outputs, enabling responsive, weight-based audiovisual interaction.
